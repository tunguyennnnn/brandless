type Product {
  id: ID!
  productId: String!
  name: String!
}

type ProductConnection {
  totalCount: Int!
  pageInfo: PageInfo!
  edges: [ProductEdge]
}

type ProductEdge {
  node: Product
  cursor: Cursor!
}

extend type Query {
  products(limit: Int): ProductConnection!
}
